import { SectionPage, sectionMetadata } from '@/app/lib/section-page';

export const metadata = sectionMetadata('work-method');

export default function WorkMethodPage() {
  return <SectionPage section="work-method" />;
}
