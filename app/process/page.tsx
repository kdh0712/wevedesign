import { SectionPage, sectionMetadata } from '@/app/lib/section-page';

export const metadata = sectionMetadata('process');

export default function ProcessPage() {
  return <SectionPage section="process" />;
}
