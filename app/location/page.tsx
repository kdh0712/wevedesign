import { SectionPage, sectionMetadata } from '@/app/lib/section-page';

export const metadata = sectionMetadata('location');

export default function LocationPage() {
  return <SectionPage section="location" />;
}
